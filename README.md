# QObject.qml

QtObject use the default property, so it can hold children.


```
    QObject {
        id: qObject
        QObject{ }
        QtObject{ }
        Timer { }
        Settings{ }
    }
```
